interface Settings {
  accessKeyId: string;
  secretAccessKey: string;
  region: string;
  Engine: string;
  LanguageCode: string;
  SampleRate: string;
  TextType: string;
  OutputFormat: string;
  VoiceId: string;
  outputDirectory: string;
  speed: string;
}

export default Settings;
